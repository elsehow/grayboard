{
  "name": "grayboard",
  "version": "0.0.0",
  "description": "the gray area between a whiteboard and a computer program",
  "main": "app/entry.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-dev": "watchify app/entry.js -o dist/bundle.js",
    "build-production": "browserify app/entry.js -o dist/bundle.js && html-inline dist/index.html -o app.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jquery": "^2.1.4"
  }
}
